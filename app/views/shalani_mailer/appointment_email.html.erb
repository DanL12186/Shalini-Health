<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>

  <body>
    <h2>
      You have a new appointment on <%= @appointment_date %>, with <%= "#{@user.first_name} #{@user.last_name}" %>. 
    </h2>

    <h3>
      Patient name: <%= "#{@user.first_name} #{@user.last_name}" %>
    </h3>

    <h4>
      Contact: <%= @user.cell %>
    </h4>

    <p>
      Don't forget to add to your calendar!
    </p>
  </body>

</html>